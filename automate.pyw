# Run hier de commands om alle automation scripts te runnen
import os

# TODO add unix commands and a way to check if the script is on a windows or unix os

# Commands for windows OS
os.system("pythonw download-flows\\flowr-win.pyw")
os.system("pythonw dir-organizer\\download-cleanr.py")
